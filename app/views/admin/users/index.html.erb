<h1>全ユーザー一覧</h1>

<ul>
  <% @users.each do |user| %>
    <li>
      <%= user.name %> - <%= user.email %>
    </li>
  <% end %>
</ul>

<ul>
  <% @users.each do |user| %>
    <li>
      <%= user.name %> - <%= user.email %>
      <!-- リンクを追加 -->
      <%= link_to '削除', admin_user_path(user), method: :delete, data: { confirm: '本当に削除しますか？' } %>
    </li>
  <% end %>
</ul>