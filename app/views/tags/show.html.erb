<h1>タグ: <%= @tag.name %></h1>

<% @posts.each do |post| %>
  <div class="post">
    <h2><%= link_to post.title, post_path(post) %></h2>
    <p><%= truncate(post.body, length: 100) %></p>
    
    <% if post.image.attached? %>  <!-- 画像が添付されているか確認 -->
      <%= image_tag post.image %>  <!-- 投稿の画像を表示 -->
    <% end %>
    
  </div>
<% end %>
